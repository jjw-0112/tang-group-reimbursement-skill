# 课题组个人集中报销 Skill 使用说明

本说明随分享包和 GitHub 仓库一起提供。Skill 帮每位学生整理自己的发票和附件，生成个人明细 Excel、补件提醒 PDF、文件对照表及带版本号的 ZIP。它只做离线核对，不代替负责人或财务审核，也不进行税务平台验真。

## 1. 安装

推荐在 Codex 中输入：

> `$skill-installer` 请安装 `https://github.com/jjw-0112/tang-group-reimbursement-skill` 仓库根目录的 Skill，名称设为 `tang-group-reimbursement`。

也可手动安装：

1. 解压分享包，找到完整的 `tang-group-reimbursement` 文件夹。保留其中的 `SKILL.md`、`scripts`、`references`、`agents`、`requirements.txt`；不要只复制 `SKILL.md`。
2. 把该文件夹放到 Codex 的个人技能目录：

   | 系统 | 推荐位置 |
   | --- | --- |
   | Windows | `%USERPROFILE%\.agents\skills\tang-group-reimbursement` |
   | macOS / Linux | `~/.agents/skills/tang-group-reimbursement` |

   如果你的 Codex 已使用 `~/.codex/skills`（本机安装在此处），也可放到该目录。同一台电脑只放一份，避免同名 Skill 重复出现。
3. 安装 Python 3.10 或更新版本，在终端执行：

   ```bash
   python -m pip install -r "<技能目录>/requirements.txt"
   ```

   macOS / Linux 上命令可能是 `python3`。可让 Codex 帮你找到解压后的绝对路径并运行安装命令。
4. 扫描件识别需要本地 Tesseract OCR 及 `chi_sim`、`eng` 语言包；RAR 读取需要 7-Zip/`7z`。在技能目录执行 `python scripts/reimburse.py doctor` 检查环境。缺少这些工具时，相关文件会列为待核实。
5. 如果 Codex 没显示 `$tang-group-reimbursement`，重启 Codex 再试。[OpenAI 官方文档](https://learn.chatgpt.com/docs/build-skills)说明 Codex 会自动发现新安装的 Skill。

## 2. 准备材料

把本次报销的发票与附件放在一个**原材料文件夹**中。支持 PDF、OFD、图片，以及 ZIP/RAR 中的文件。运单、行程单、付款记录、入库单、论文首页等附件与相关发票放在一起，清楚的文件名有助于自动匹配。

准备好姓名、学号、电话、**本次报销截止日期**，并确认哪些发票属于**对公转账**。可选准备历史报销目录，用于提示相同发票号码。不要把以往生成的报销 ZIP 当作本次原材料；保留旧版本供自己核对即可。

## 3. 首次运行

在 Codex 中输入，例如：

> `$tang-group-reimbursement` 请整理我在 `D:\报销材料\本次` 文件夹中的集中报销材料。报销截止日期是 `2026-09-30`。先向我收集个人信息，并一次性确认对公转账发票。

Codex 会先扫描材料，生成可复核的 `review.json`，再根据你提供的信息和原件证据生成交件包。对识别不清、金额冲突或附件关联不明确的文件，保持“待核实”，不要要求它猜测。

输出目录默认在原材料文件夹旁，名称为 `<原材料文件夹名>_报销输出`。其中包含 `state.json` 和 `review.json`；每次完成后会新增 `v1`、`v2` 等版本目录及对应 ZIP。**不要删除 `state.json`**，补件续跑需要它。

## 4. 查看结果与补件

| 文件或状态 | 你要检查什么 |
| --- | --- |
| 个人报销 Excel | 姓名、学号、电话、金额、费用类别、全局编号；“缺件”票只算暂计金额。 |
| `补件与复核提醒.pdf` | 逐项查看缺少的附件、待核实问题、不可报销事项和线下提交提醒。显示“尚不可提交”时先处理问题。 |
| `文件对照表.csv` | 原文件、发票号码、状态、新编号和新文件名是否对应。 |
| `待核实`、`不可报销` 文件夹 | 原名副本保留供核对；它们不计入 Excel 暂计金额。 |
| `编号变更.csv` | 从第二版起核对旧编号与新编号，尤其是更正发票。 |

补齐材料后，**向同一个原材料文件夹追加文件**，不要改动旧版输出。再次在 Codex 输入：

> `$tang-group-reimbursement` 我已向 `D:\报销材料\本次` 补充了运单和付款记录；请使用原来的输出目录续跑，复查未结问题并生成新版本。

发现疑似更正发票时，Codex 会请你确认旧票与新票的替换关系。确认后才会替换、重新编号。若材料仍有疑点，可继续补充并生成 v3、v4。

## 5. 常见问题

- **图片无法识别**：检查 `doctor` 是否显示 OCR 可用；需要 Tesseract 的简体中文及英文语言包。无法读清时可补原 PDF 或清晰扫描件。
- **RAR 无法读取**：安装 7-Zip/`7z`，或先自行解压到原材料文件夹。
- **文件名金额和票面不同**：以发票正文为核对依据，该票先列待核实；核对付款记录后再确认。
- **票据合规但缺行程单、运单、入库单等**：仍会进入 Excel 暂计金额，但提醒 PDF 会标注缺件，补齐后续跑。
- **新增材料后编号变化**：查看新版 `编号变更.csv`；交件时只使用同一版本的 Excel 和发票副本。
- **手册规则更新**：请课题组统一更新并重新分发 Skill。直接替换 Skill 文件夹不会删除你原材料旁的续跑状态。

分享包只有规则摘要、处理脚本与测试，不含任何学生的原始发票或个人报销结果。每位同学在自己的电脑上处理并自行交件。
